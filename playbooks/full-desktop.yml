---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - build
    - server
    - desktop
    - games
    - i3
  post_tasks:
    - name: Create ~/.playbook.provision
      copy: content=full-desktop dest=~/.playbook.provision
