---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - build
    - docker
    - desktop
    - games
    - gnome
  pre_tasks:
    - name: Create ~/.playbook.provision
      copy: content=full-desktop dest=~/.playbook.provision mode=0644
