---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - build
    - server
    - desktop
  post_tasks:
    - name: Create ~/.playbook.provision
      copy: content=dev-desktop dest=~/.playbook.provision
