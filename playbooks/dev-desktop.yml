---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - build
    - server
    - desktop
    - i3
  post_tasks:
    - name: Create ~/.playbook.provision
      copy: content=dev-desktop dest=~/.playbook.provision
