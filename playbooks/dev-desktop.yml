---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - c
    - go
    - node
    - php
    - python
    - docker
    - desktop
    - emacs
    - gnome
  pre_tasks:
    - name: Create ~/.playbook.provision
      copy: content=dev-desktop dest=~/.playbook.provision mode=0644
