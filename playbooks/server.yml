---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - server
  post_tasks:
    - name: Create ~/.playbook.provision
      copy: content=server dest=~/.playbook.provision
