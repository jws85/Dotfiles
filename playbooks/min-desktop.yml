---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - desktop
    - mate
  post_tasks:
    - name: Create ~/.playbook.provision
      copy: content=min-desktop dest=~/.playbook.provision
