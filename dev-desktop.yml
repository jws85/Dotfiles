---
- hosts: localhost
  connection: local
  gather_facts: yes
  roles:
    - base
    - build
    - docker
    - desktop
    - mpd
    - emacs
    - gtk-flat
    - i3
  pre_tasks:
    - name: Create ~/.playbook.provision
      copy: content=dev-desktop dest=~/.playbook.provision mode=0644
